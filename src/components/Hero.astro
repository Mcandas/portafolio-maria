---
import heroImage from "@assets/images/fotoperfil.png";
import { Image } from '@astrojs/image/components';
import Button from '@components/ui/Button.astro';
---

<section
	data-testid="hero"
	class="flex h-screen flex-col-reverse items-center justify-center text-center lg:flex-row"
>
	<div class="mb-10 flex flex-col items-center lg:mb-0 lg:w-1/2">
		<div>
			<h1 class="mb-10 font-monospace text-3xl text-black dark:text-white lg:text-5xl">
				💫¡Hola! Me llamo <span class="text-orange">María</span> soy desarrolladora y diseñadora web de mentirijilla.
			</h1>
		</div>
	</div>
	<div data-testid="hero-img" class="mb-10 flex w-1/2 items-center justify-center">
        <Image
            src={heroImage}
            alt="Foto de perfil"
            aspectRatio="1:1"
            format="jpg"
            width={400}
            height={400}
            loading="eager"
            style={{ borderRadius: '50%', maxWidth: '100%', height: 'auto' }}
        />
    </div>
</section>
